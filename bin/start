#!/usr/bin/env node

require('shelljs/global');

if (process.env.NPM_CONFIG_PRODUCTION) {
    exec('npm run www');
} else {
    exec('npm run webpack-dev-server -- --inline --progress --port 8080')
}
